export default function Layout({ children }) {
  return (
    <div className="max-w-4xl mx-auto px-6">
      <header className="py-6 text-xl font-bold text-center">My Portfolio</header>
      {children}
      <footer className="py-6 text-center text-sm text-gray-500">
        © {new Date().getFullYear()} Your Name. All rights reserved.
      </footer>
    </div>
  );
}

import { useTheme } from "next-themes";

export default function Layout({ children }) {
  const { theme, setTheme } = useTheme();

  return (
    <div className="max-w-4xl mx-auto px-6">
      <header className="py-6 text-xl font-bold text-center flex justify-between">
        My Portfolio
        <button onClick={() => setTheme(theme === "light" ? "dark" : "light")}>
          🌙 / ☀️
        </button>
      </header>
      {children}
      <footer className="py-6 text-center text-sm">© {new Date().getFullYear()} Your Name</footer>
    </div>
  );
}
